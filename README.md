# 📱 متجر الهواتف (Vevo Store)

تطبيق متجر إلكتروني متكامل لبيع الهواتف وملحقاتها، مبني باستخدام إطار العمل **Laravel**.

## ✨ المميزات

يحتوي المشروع على العديد من المميزات للمستخدمين والمسؤولين:

### 👤 للمستخدمين:
- **تصفح المنتجات:** عرض أحدث المنتجات والمنتجات الأكثر مبيعاً.
- **سلة التسوق:** إضافة منتجات للسلة، تحديث الكميات، وحذف المنتجات.
- **المفضلة:** قائمة رغبات لحفظ المنتجات المفضلة.
- **حساب المستخدم:** إمكانية تعديل الملف الشخصي وتغيير كلمة المرور.
- **المراجعات:** إمكانية إضافة مراجعات للمنتجات.
- **النشرة البريدية:** الاشتراك لاستقبال آخر الأخبار.

### 🛡️ للمسؤولين (Admin):
- **لوحة تحكم خاصة:** إدارة كاملة للمتجر.
- **إدارة المنتجات:** إضافة، تعديل، وحذف المنتجات.
- **نظام حماية:** تسجيل دخول آمن للمسؤولين.

## 🛠️ التقنيات المستخدمة

- **الواجهة الخلفية (Backend):** Laravel 11.x, PHP 8.2+
- **قاعدة البيانات (Database):** MySQL / SQLite
- **الواجهة الأمامية (Frontend):** Blade Templates, TailwindCSS (via Vite)

## 🚀 طريقة التثبيت والتشغيل

اتبع الخطوات التالية لتشغيل المشروع على جهازك:

1. **نسخ المشروع:**
   ```bash
   git clone <repository-url>
   cd phones_store_app
   ```

2. **تثبيت مكتبات PHP:**
   ```bash
   composer install
   ```

3. **تثبيت مكتبات الواجهة الأمامية:**
   ```bash
   npm install
   ```

4. **إعداد ملف البيئة:**
   نسخ ملف المثال `.env.example` إلى `.env`:
   ```bash
   cp .env.example .env
   ```
   ثم قم بإعداد بيانات قاعدة البيانات في ملف `.env`.

5. **توليد مفتاح التطبيق:**
   ```bash
   php artisan key:generate
   ```

6. **تجهيز قاعدة البيانات:**
   ```bash
   php artisan migrate --seed
   ```
   *(هذا الأمر سيقوم بإنشاء الجداول وإضافة بيانات أولية، مثل حساب الأدمن)*.

7. **تشغيل المشروع:**
   - لتشغيل السيرفر المحلي:
     ```bash
     php artisan serve
     ```
   - لتشغيل تجميع ملفات الواجهة الأمامية (في نافذة منفصلة):
     ```bash
     npm run dev
     ```

## 🔑 حساب المسؤول الافتراضي (للتجربة)

إذا تم استخدام الـ Seeder، يمكنك تجربة الدخول كمسؤول باستخدام البيانات الموجودة في `database/seeders/AdminUserSeeder.php`.

---
.